---
# Restart, never reload: CoreDNS reads the TLS certificate and key once, at
# startup, so SIGUSR1 would keep serving the old pair after the files change.
- name: Restart coredns
  ansible.builtin.systemd_service:
    name: coredns
    state: restarted
    daemon_reload: true
